import React from "react";
import ReactDOM from "react-dom/client";
import MapComponent from "./App.jsx";  // Ensure correct import

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MapComponent />
  </React.StrictMode>
);
